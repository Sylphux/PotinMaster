<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>User page</title>

    <!-- Lien Bootstrap CSS -->
    <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">
</head>

<body class="text-center">
    <header>
        <nav>
            <div>
                <a class="nav-link" href="/welcome">Accueil</a>
                <a class="nav-link" href="/team">Team</a>
                <a class="nav-link" href="/contact">Contact</a>
                <a class="nav-link" href="/welcome/1">Profil Utilisateur</a>
            </div>
        </nav>
    </header>

    <main>
        <%if params[:name].scan(/[a-zA-Z]/).size == 0%>
            <%found_user = User.find(params[:name].to_i)%>
        <%else%>
            <%found_user = User.where(first_name: params[:name]).first %>
        <%end%>
        <h1><%= "Bienvenue sur ta page profil #{found_user.first_name}" %></h1>
        <a><%= "Ton nom de famille :  #{found_user.last_name}" %></a><br>
        <a><%= "Ta ville :  #{found_user.city.city_name}" %></a><br>
        <a><%= "Ton age :  #{found_user.age}" %></a><br>
        <a><%= "Ton email :  #{found_user.email}" %></a><br>
        <a><%= "Date de création de ton compte :  #{found_user.created_at}" %></a><br>
    </main>

<!-- Script Bootstrap JS -->
    <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous">
    </script>
</body>
</html>