<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <title>Gossip team</title>
    </head>

    <body>
        <main>
            <h1>Bienvenue sur Gossip THP !</h1>
            <h2>Découvre ici toute la liste des potins déjà rédigés !</h2>
            <br>
            <div class="article">
                <%for n in Gossip.all do %>
                    Titre du gossip : <%= n.title%><br>
                    Utilisateur : <%= "#{n.user.first_name} #{n.user.last_name}" %><br>
                    Contenu : <%= n.content%><br>
                    <a href="/gossips/<%= n.id %>">Voir ce Gossip</a>
                    <br><br>
                <%end%>
            </div>
        </main>
    </body>
</html>